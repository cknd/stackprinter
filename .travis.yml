
dist: xenial   # required for Python >= 3.7

language: python
python:
  - "3.5"
  - "3.6"
  - "3.7"
  - "3.8-dev"

# command to install dependencies
install:
  - pip install .[tests]

# command to run tests
script:
  - python -m pytest